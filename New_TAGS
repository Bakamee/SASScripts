pipeline{

    agent {label "sasmachine"}
    environment{
    def tag = env.ref.tokenize('/')[-1]
    var="null"
    }
    stages{
        
        stage ("New Code Developed"){
            steps{
                echo "new dev code commited, needs to be productionised"
                 sh "echo ${tag}"
            }
        }
        stage("Checkout  to Prod"){
            steps {
             echo "checkout steps"
            }
        }
        
    }
}
