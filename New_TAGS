pipeline {
    agent any
    stages {
        stage('notify') {
            when {
                branch "/origin/dev"
            }
            steps {
                sh "printenv"
            }
        }
    }
}
